# Demo script — approximately 2:20

## 0:00–0:18

“Computational thinking is usually introduced at a keyboard. Barre Code starts somewhere else: with sequence, state, parameters, repetition, and resolution already present in movement vocabulary.”

## 0:18–0:42

“This is one standalone lesson. Six synthetic movement ciphers are out of order. I can drag them, or use the accessible earlier and later buttons. I’ll load the solved demo so you can see the complete loop.”

## 0:42–1:02

“Compile runs five deterministic TypeScript rules. The model does not decide whether this is correct. It cannot score a person, and no movement is captured. The compiler verifies dependencies and produces exact repairs when structure fails.”

## 1:02–1:25

“Next, the lab inserts a glitch. Relevé changes state before Dégagé defines the accelerated pathway. Finding the error turns debugging into a visible learning act.”

## 1:25–1:47

“GPT-5.6 Sol is optional. The server revalidates first, then asks the Responses API for a structured explanation. If the key, quota, model, or schema fails, deterministic coaching appears and the learner still completes the lesson.”

## 1:47–2:05

“The learner changes one bounded quality parameter while preserving structure, then creates a Learning Receipt. It records lesson logic only, with explicit limits: no physical performance, safety, health, biometric, or artistic evaluation.”

## 2:05–2:20

“Barre Code shows a different authority model for AI education: code validates, AI explains, and the human keeps moving. The body can illuminate computation without becoming a dataset.”
