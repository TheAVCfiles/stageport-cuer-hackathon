{
  "name": "stageport-cueroom",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A human-approved GPT-5.6 choreography and embodied-learning agent built with Codex for OpenAI Build Week 2026.",
  "scripts": {
    "dev": "node --watch server.mjs",
    "start": "node server.mjs",
    "test": "node --test",
    "check": "node --check server.mjs && node --check public/app.js && node --check api/compose.mjs && node --check api/approve.mjs && node --check api/health.mjs && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "openai": "^6.48.0"
  }
}
